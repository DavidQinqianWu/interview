{
	"name": "interview",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"vuepress": "^1.8.2"
	},
	"scripts": {
		"docs:dev": "vuepress dev docs",
		// 先执行 build
		"docs:build": "vuepress build docs",
		// 在执行 deploy 会自动更新到gitpage
		"docs:deploy": "gh-pages -d dist"
	},
	"devDependencies": {
		"gh-pages": "^3.1.0"
	}
}
